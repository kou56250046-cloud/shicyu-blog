# 01 プロジェクト初期化

## 概要
Next.js プロジェクトを作成し、必要なパッケージとディレクトリをセットアップする。

---

## Todo

### Next.js セットアップ
- [×] `npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*"` を実行
- [×] `npm install gray-matter remark remark-html` を実行
- [ ] ~~`npm install -D playwright @playwright/test ts-node`~~ → MCP Playwright を使うため不要
- [ ] ~~`npx playwright install`~~ → MCP Playwright を使うため不要

### ディレクトリ作成
- [×] `readings/` を作成
- [×] `screenshots/` を作成（ローカル作業用・.gitignore に追加）
- [×] `public/screenshots/` を作成（git 管理対象）
- [×] `scripts/` を作成（MCP 方式のため capture.ts は作成しない）

### .gitignore 設定
- [×] `screenshots/` をルートの .gitignore に追記（ローカル作業用スクショは除外）
- [×] `.env.local` が .gitignore に含まれていることを確認（`create-next-app` で `.env*` として追加済み）

### 動作確認
- [×] `npm run dev` でエラーなく起動することを確認（HTTP 200 確認済み）
- [×] `npm run build` でエラーなくビルドできることを確認

---

## 参照
- CLAUDE.md「初期化手順」セクション
