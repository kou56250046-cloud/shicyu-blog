# 05 ルートレイアウト実装

## 概要
`app/layout.tsx` を実装する。フォント設定・グローバルメタデータ・共通ナビゲーションを含む。
全ページの土台となるため、Next.js のベストプラクティスを忠実に守ること。

---

## Todo

### app/layout.tsx
- [ ] `next/font/google` で `Noto_Sans_JP` を設定（`display: 'swap'`）
- [ ] `<html lang="ja">` にフォントの `className` を適用
- [ ] サイト全体の `metadata` オブジェクトを export
  - `title`: "四柱推命鑑定ブログ"
  - `description`: "個人・相性の四柱推命鑑定記事"

### 共通ナビゲーション（components/Header.tsx）
- [ ] サイトタイトルリンク（`/`）
- [ ] ナビリンク: 鑑定一覧（`/`）・相性鑑定（`/compatibility`）・人物一覧（`/people`）
- [ ] レスポンシブ対応（スマホ・PC）

### app/not-found.tsx
- [ ] 404 ページを作成

### 動作確認
- [ ] `npm run dev` でレイアウトが正しく表示されることを確認
- [ ] スマホ幅でナビが崩れないことを確認

---

## 参照
- CLAUDE.md「Next.js ベストプラクティス > フォント」セクション
- CLAUDE.md「Next.js ベストプラクティス > メタデータ」セクション
- CLAUDE.md「Next.js ベストプラクティス > ファイル規約」セクション
