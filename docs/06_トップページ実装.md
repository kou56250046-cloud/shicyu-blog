# 06 トップページ実装（`/`）

## 概要
鑑定記事の一覧を表示するトップページ。
人物別タブで絞り込み、日付降順で記事カードを表示する。

---

## Todo

### app/page.tsx（Server Component）
- [×] `getAllReadings()` でデータ取得（サーバーサイド）
- [×] `metadata` を export（title: "鑑定一覧"）
- [×] 記事カード一覧を日付降順でレンダリング

### 記事カード（components/ReadingCard.tsx）
- [×] タイトル・日付・格局・用神を表示
- [×] 個別記事ページ（`/readings/[slug]`）へのリンク
- [×] `type === "compatibility"` の場合は相性バッジを表示

### 人物別タブ（components/PersonTabs.tsx）
- [×] 「すべて」タブ + 人物ごとのタブを生成
- [×] タブ切り替えで記事一覧を絞り込む（`'use client'`）
- [×] Server Component の `page.tsx` から人物リストと記事をpropsで受け取る

### app/loading.tsx
- [×] スケルトンUIを作成（Tailwind `animate-pulse`）

### 動作確認
- [×] `npm run build` でエラーなく静的生成完了
- [ ] `npm run dev` でサンプル記事の一覧・タブ切り替えを目視確認

---

## 参照
- CLAUDE.md「Next.js ベストプラクティス > Server/Client Components の使い分け」セクション
- CLAUDE.md「Next.js ブログアプリ仕様 > ページ一覧」
