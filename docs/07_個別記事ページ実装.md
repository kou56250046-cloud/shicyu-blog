# 07 個別記事ページ実装（`/readings/[slug]`）

## 概要
1件の鑑定記事を表示するページ。
スクリーンショット画像 + Markdown コンテンツを表示する。
全記事をビルド時に静的生成する。

---

## Todo

### app/readings/[slug]/page.tsx（Server Component）
- [×] `generateStaticParams()` を実装（`getAllReadingSlugs()` を使用）
- [×] `generateMetadata()` を実装（記事タイトルを動的に設定）
- [×] `getReading(slug)` でデータ取得
- [×] `next/image` の `<Image>` でスクリーンショットを表示（`fill` + `object-contain`）
- [×] HTML 変換済みコンテンツを `dangerouslySetInnerHTML` でレンダリング
- [×] フロントマター情報（生年月日・格局・用神・タグなど）をヘッダー部に表示

### スタイリング
- [×] `.prose-reading` カスタム CSS で見出し・テーブル・リストを装飾（globals.css）
- [×] h2 は indigo ボーダー、テーブルはストライプ表示

### app/readings/[slug]/loading.tsx
- [×] スケルトン UI を作成

### app/readings/[slug]/error.tsx（`'use client'` 必須）
- [×] エラーバウンダリを作成（再試行ボタン付き）

### 動作確認
- [×] `npm run build` で `/readings/2026-04-05_Kohei` が静的生成されることを確認
- [ ] ブラウザで `/readings/2026-04-05_Kohei` にアクセスして表示を目視確認

---

## 参照
- CLAUDE.md「Next.js ベストプラクティス > 静的生成」セクション
- CLAUDE.md「Next.js ベストプラクティス > 画像最適化」セクション
- CLAUDE.md「mdファイルのフォーマット仕様 > セクション構成」
