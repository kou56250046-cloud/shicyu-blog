# 08 相性鑑定一覧ページ実装（`/compatibility`）

## 概要
`type === "compatibility"` の記事のみを一覧表示するページ。
2人のスクリーンショットを並べて表示する。

---

## Todo

### app/compatibility/page.tsx（Server Component）
- [×] `getCompatibilityReadings()` でデータ取得
- [×] `metadata` を export（title: "相性鑑定一覧"）
- [×] 記事がない場合の空状態UI を表示（💑 アイコン付き）

### 相性カード（components/CompatibilityCard.tsx）
- [×] `person1` と `person2` の名前を並べて表示
- [×] `screenshot1` と `screenshot2` を `next/image` で横並び表示（画像なし時はプレースホルダー）
- [×] 日付・タグを表示
- [×] 個別記事ページへのリンク

### app/compatibility/loading.tsx
- [×] スケルトン UI を作成

### 動作確認
- [×] `npm run build` でエラーなく静的生成完了
- [ ] 相性鑑定 md を追加してブラウザで表示を目視確認

---

## 参照
- CLAUDE.md「相性鑑定のファイル命名規則」セクション
- CLAUDE.md「Next.js ベストプラクティス > 画像最適化」セクション
