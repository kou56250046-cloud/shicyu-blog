
# 09 人物一覧ページ実装（`/people`）

## 概要
鑑定記事に登場する人物の一覧と、各人物の記事へのリンクを表示するページ。

---

## Todo

### app/people/page.tsx（Server Component）
- [×] `getAllPeople()` で人物一覧を取得
- [×] 各人物ごとに `getReadingsByPerson(person)` で記事一覧を取得
- [×] `metadata` を export（title: "人物一覧"）

### 人物カード（components/PersonCard.tsx）
- [×] 人物名・記事数を表示
- [×] 個人鑑定 / 相性鑑定の内訳バッジを表示
- [×] 最新記事の日付・格局・用神を表示
- [×] `/?person=名前` でトップページのタブ絞り込みにリンク

### app/people/loading.tsx
- [×] スケルトン UI を作成

### PersonTabs 更新
- [×] `useSearchParams` で URL パラメータを読み取り、`/?person=名前` リンクに対応
- [×] `<Suspense>` で `page.tsx` をラップ（useSearchParams 要件）

### 動作確認
- [×] `npm run build` でエラーなく静的生成完了
- [ ] ブラウザで `/people` → 人物カードクリック → タブ絞り込みを目視確認

---

## 参照
- CLAUDE.md「Next.js ブログアプリ仕様 > ページ一覧」
- CLAUDE.md「Next.js ベストプラクティス > Server Components」セクション
